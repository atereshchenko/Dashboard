@model Dashboard.Models.Tile
@{
    Layout = "_Admin";
}

<form role="form" asp-action="create" asp-controller="tile" class="row g-3 needs-validation" novalidate>
    @Html.HiddenFor(m => m.Id)
    <div class="col-md-9">
        <label asp-for="@Model.Name" class="form-label">Наименование</label>
        <input asp-for="@Model.Name" type="text" class="form-control" required>
        <div class="invalid-feedback">
           Укажите наименование.
        </div>
    </div>
    <div class="col-md-3">
        <label asp-for="@Model.Number" class="form-label">Порядковый номер отображения</label>
        <input asp-for="@Model.Number" type="number"  class="form-control" required>
        <div class="invalid-feedback">
            Укажите порядковый номер отображения плитки
        </div>
    </div>    
    <div class="col-md-12">
        <label asp-for="@Model.Description" class="form-label">Описание</label>
        <input asp-for="@Model.Description" type="text" class="form-control" required>
        <div class="invalid-feedback">
            Укажите описание.
        </div>
    </div>    
    <div class="col-md-6">
        <label asp-for="@Model.Link" class="form-label">Ссылка</label>
        <input asp-for="@Model.Link" type="text" class="form-control" required>
        <div class="invalid-feedback">
            Укажите ссылку.
        </div>
    </div>
    <div class="col-md-3">
        <label asp-for="@Model.LinkName" class="form-label">Текст ссылки</label>
        <input asp-for="@Model.LinkName" type="text" class="form-control" required>
        <div class="invalid-feedback">
            Укажите наименоввание ссылки.
        </div>
    </div>
    <div class="col-md-3">
        <label asp-for="@Model.SvgClass" class="form-label">SVG-класс</label>
        <input asp-for="@Model.SvgClass" type="text" class="form-control">
        <div class="invalid-feedback">
            Укажите SVG-класс иконки.
        </div>
    </div>
    <div class="col-md-12">
       <input asp-for="@Model.TurnOn" type="checkbox" class="btn-check" id="btncheck1" autocomplete="off">
       <label asp-for="@Model.TurnOn" class="btn btn-sm btn-outline-secondary" for="btncheck1">Вкл/выкл</label>  
    </div>

    <div class="col-12">
        <button class="btn btn-success" type="submit">Сохранить</button>
    </div>
</form>

@section CustomScripts
{
<script>
    $('#tilesmenu>a').addClass("active");
    (function () {'use strict'
    // Получите все формы, к которым мы хотим применить пользовательские стили проверки Bootstrap
    var forms = document.querySelectorAll('.needs-validation')

    // Зацикливайтесь на них и предотвращайте отправку
    Array.prototype.slice.call(forms)
        .forEach(function (form) {
            form.addEventListener('submit', function (event) {
                if (!form.checkValidity()) {
                    event.preventDefault()
                    event.stopPropagation()
                }
                form.classList.add('was-validated')
            }, false)
        })
     })()
</script>
}
